n = 10
n %% 3
n / 1000
floor(n / 1000)
floor(n / 10)
floor(n / 1)
floor(n / 2.5)
floor(n / 2)
floor(n / 1.3)
floor(14 / 1.3)
f* <- function(n) n
f* <- function(n){
return n;
}
f <- function(n) {
return n;
}
f <- function(n) {
return n
}
f* <- function(n) {
return(n)
}
f* <- function(n) {
return <- n
}
f <- function(n) n
f* <- function(n) n
f_ast <- function(n) n
View(f)
f_ast <- function(n) {
return 0
}
f_ast <- function(n) {
return = 0
}
f_ast <- function(n) {
return <- 0
}
f_ast <- function(n) {
if ((floor(n / 1000) %% 2 == n %% 4 == 0))
return = 0
}
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
}
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
secnd_cond = n %% 4 == 0
}
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
secnd_cond = n %% 4 == 0
print(first_cond)
}
f_ast(5)
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
secnd_cond = n %% 4 == 0
if (first_cond == secnd_cond)
}
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
secnd_cond = n %% 4 == 0
if (first_cond == secnd_cond)
return 1
else
return 0
}
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
secnd_cond = n %% 4 == 0
if (first_cond == secnd_cond) {
return 1
}
else
{
return 0
}
}
f_ast <- function(n) {
first_cond = floor(n / 1000) %% 2 == 0
secnd_cond = n %% 4 == 0
if (first_cond == secnd_cond) {
return = 1
}
else
{
return = 0
}
}
values = [1..10000]
values = seq(1, 10000)
f_ast(values)
values = seq_len(10000)
sample(1:10000)
f_ast(1:10000)
f_ast(3200)
f_ast(3200)
print(f_ast(3200))
print(f_ast(3210))
lapply(values, f_ast)
lapply(values, f_ast)
values = lapply(values, f_ast)
values[(0,10)]
values[(0:10)]
View(f_ast)
View(f)
View(f)
View(f)
View(f)
x = f_ast(34)
x = f_ast(3400)
x = f_ast(34000)
values
install.packages("e1071","rpart")
library(rpart)
library(rpart)
#We start with building a decision tree for a set of data - lymphography
#install.packages("e1071","rpart")
library(e1071)
library(rpart)
dataSet = read.table(file = "lymphography_processed.data", header = T, sep='\t', row.names=NULL)
dim(dataSet)
dataSet = read.table(file = "lymphography_processed.data", header = T, sep='\t', row.names=NULL)
pwd
#We start with building a decision tree for a set of data - lymphography
#install.packages("e1071","rpart")
library(e1071)
library(rpart)
dataSet = read.table(file = "/home/robert/Code/SUS/7/lymphography_processed.data", header = T, sep='\t', row.names=NULL)
